{
  "name": "nodo-operativo",
  "version": "2.1.3",
  "description": "NODO · OPERATIVO — Panel de operaciones",
  "main": "main.js",
  "scripts": {
    "panel:build": "node scripts/build-panel.cjs",
    "panel:check": "node scripts/build-panel.cjs --check",
    "panel:smoke": "node scripts/smoke-panel.cjs",
    "test": "node --test tests/*.test.cjs",
    "prestart": "npm run panel:build",
    "predev": "npm run panel:build",
    "prebuild": "npm run panel:build",
    "prebuild:win": "npm run panel:build",
    "prerelease": "npm run panel:build",
    "start": "set \"ELECTRON_RUN_AS_NODE=\" && electron . --remote-debugging-port=9222",
    "dev": "set \"ELECTRON_RUN_AS_NODE=\" && electron . --dev --remote-debugging-port=9222",
    "build": "electron-builder",
    "build:win": "electron-builder --win",
    "release": "electron-builder --win --publish always"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.111.0",
    "electron-updater": "^6.8.9"
  },
  "devDependencies": {
    "electron": "^32.3.3",
    "electron-builder": "^25.1.0"
  },
  "build": {
    "appId": "com.nodooperativo.app",
    "productName": "NODO · OPERATIVO",
    "publish": [
      {
        "provider": "github",
        "owner": "zhinouno-ui",
        "repo": "NexoBetaChan"
      }
    ],
    "files": [
      "main.js",
      "main/**/*",
      "renderer/generated/**/*",
      "app-preload.js",
      "agent-preload.js",
      "agent-preload-bet300.js",
      "chunior-bridge.js",
      "config.json",
      "services/**/*",
      "shared/**/*",
      "NODO · OPERATIVO LITE.htm",
      "index.html",
      "nodoSuperior/**/*",
      "icons/**/*",
      "node_modules/**/*"
    ],
    "win": {
      "icon": "icons/icon-n.png",
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        },
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        }
      ],
      "certificateFile": null,
      "certificatePassword": null
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "artifactName": "NODO-OPERATIVO-Setup-${version}.${ext}"
    },
    "portable": {
      "artifactName": "NODO-OPERATIVO-${version}.${ext}"
    }
  }
}
